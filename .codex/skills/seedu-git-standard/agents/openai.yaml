interface:
  display_name: "SE-EDU Git Standard"
  short_description: "Apply project Git commit conventions"
  default_prompt: "Use $seedu-git-standard when preparing this commit or branch name."

policy:
  allow_implicit_invocation: true
